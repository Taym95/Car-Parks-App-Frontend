import React, {PropTypes, Component} from 'react/addons';

export default class Marker extends Component {

    render() {
        return (
            <div>
                <img src=/>
            </div>
        );
    }
}